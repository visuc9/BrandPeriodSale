@using Driven.App.BrandPeriodSalesReporting.Models
@using Driven.App.BrandPeriodSalesReporting.Helpers

@model EditDenialTypeViewModel

<div class="modal-content">

    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Edit Denial Type</h4>
    </div>

    @using (Ajax.BeginForm(new AjaxOptions { HttpMethod = "POST" }))
    {

        <div class="modal-body">
            @Html.AntiForgeryToken()
            @Html.Hidden("DialogResult", (string)Convert.ToString(ViewBag.DialogResult ?? string.Empty))
            @Html.Partial("_ValidationSummary")


            <fieldset class="form-group">
                @Html.LabelFor(m => m.DenialTypeId)
                <div>@Model.DenialTypeId</div>
            </fieldset>

            <fieldset class="form-group">
                @Html.LabelFor(m => m.DenialTypeCodeId)
                <div>@Model.DenialTypeCodeId</div>
            </fieldset>

            <fieldset class="form-group">
                @Html.LabelFor(m => m.DenialTypeDescription)
                @Html.TextBoxFor(m => m.DenialTypeDescription, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.DenialTypeDescription)
            </fieldset>

        </div>

        <div class="modal-footer">
            @Html.HiddenFor(m => m.DenialTypeId)
            @Html.HiddenFor(m => m.DenialTypeCodeId)

            <button id="SaveBtn" class="btn btn-primary" name="submit" type="submit">Save</button>
            <button type="button" class="btn btn-inverse" data-dismiss="modal">Cancel</button>
        </div>

    }

</div>

